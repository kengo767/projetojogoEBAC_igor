using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ola : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    { 
        Debug.Log("coe");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("opaa de novo");
    }
}
